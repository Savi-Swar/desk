name: collect-2x-daily
on:
  schedule:
    - cron: "22 11,23 * * *"   # 19:20 / 07:20 SGT
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: run collectors under health ledger
        run: WATCH_MIN=8 python run_with_health.py collect_daily.py arb_census.py arb_watch.py kalshi_xvenue.py limitless_xvenue.py sx_bet.py funding_carry.py relations_watch.py weather_collect.py longshot_forward.py
      - name: commit ledgers
        run: |
          git config user.name "desk-bot"
          git config user.email "actions@users.noreply.github.com"
          git config merge.ours.driver true   # lets .gitattributes merge=ours resolve derived-file races
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "collect $(date -u +%Y-%m-%dT%H:%MZ)"
          for i in 1 2 3 4 5; do
            if git pull --rebase origin main && git push origin main; then exit 0; fi
            git rebase --abort 2>/dev/null || true   # never leave a wedged rebase for the next retry
            sleep $((RANDOM % 8 + 2))
          done
          exit 1

